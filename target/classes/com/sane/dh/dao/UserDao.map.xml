<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sane.dh.dao.UserDao">
	<!-- <select id="getSpiltter" parameterType="com.sane.spring.model.Spiltter"
		resultType="com.sane.spring.model.Spiltter">
		SELECT * FROM spitter WHERE username=#{userName} AND password=#{password}
	</select> -->
	<insert id="addUser" parameterType="com.sane.dh.model.user.UserRegistInfo"
		flushCache="true">
		INSERT INTO user (email_phone,password) VALUES
		(#{email_phone},#{password})
	</insert>
	<!-- <update id="updateSpiltter" parameterType="com.sane.spring.model.Spiltter">
		UPDATE spitter SET password=#{password} WHERE id=#{id}
	</update>
	<delete id="deleteSpiltter" parameterType="int">
		DELETE FROM spitter WHERE id=#{id}
	</delete>-->
	<select id="getUserByEmail_phone" parameterType="String" resultType="com.sane.dh.model.user.UserRegistInfo">
		Select *  FROM user WHERE email_phone=#{email_phone}
	</select> 
	<select id="email_phoneIsRegisted" parameterType="String" resultType="int">
	select count(id) count from user where email_phone=#{email_phone}
	</select>
</mapper>



